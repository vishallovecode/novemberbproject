const App = () => {
  return (
    <div>
      <h2>We are just started our better career....</h2>
    </div>
  );
};

export default App;

// Here we have created a component with name App
// And we are giving him a permission to use anyhwere inside the project in line 9

// named export and default export
// each component in react should have one single parent
